[package]
name = "tyrne-userland-hello"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Tyrne first userspace program — greets via console_write, exits via task_exit (B6)."

[[bin]]
name = "hello"
path = "src/main.rs"

[dependencies]
tyrne-user = { path = "../tyrne-user" }

[lints]
workspace = true
